# In dev, the Vite proxy forwards /api to this target (see vite.config.js).
VITE_API_TARGET=http://localhost:8000

# For a production build where the API is on a different origin, set the full
# base URL used by the client (leave blank to use same-origin "/api").
VITE_API_BASE=
