fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1
httpx==0.28.1
# Optional: only needed for the Playwright scraping fallback (pass 2).
# Install with `pip install playwright && playwright install chromium`.
playwright==1.49.1

# Dev / tests
pytest==8.3.4
